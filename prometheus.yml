scrape_configs:
  - job_name: 'spring-boot-app'
    metrics_path: '/book-lib/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080'] # Địa chỉ của ứng dụng Spring Boot
rule_files:
  - "alerts.yml" # Đường dẫn đến file rule